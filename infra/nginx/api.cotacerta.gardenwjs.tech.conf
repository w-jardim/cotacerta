# Configuração Nginx para api.cotacerta.gardenwjs.tech (API)
#
# Este arquivo deve ser colocado em:
# /etc/nginx/sites-available/api.cotacerta.gardenwjs.tech
#
# E depois ativado com:
# sudo ln -s /etc/nginx/sites-available/api.cotacerta.gardenwjs.tech /etc/nginx/sites-enabled/
# sudo nginx -t
# sudo systemctl reload nginx

server {
    listen 80;
    listen [::]:80;
    server_name api.cotacerta.gardenwjs.tech;

    # Logs
    access_log /var/log/nginx/cotacerta-api-access.log;
    error_log /var/log/nginx/cotacerta-api-error.log;

    # Proxy para container API
    location / {
        proxy_pass http://127.0.0.1:3401;
        proxy_http_version 1.1;

        # Headers de proxy
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;

        # CORS (se necessário)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_cache_bypass $http_upgrade;

        # Timeout
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# Após configurar SSL com Certbot, o Certbot irá adicionar automaticamente
# o bloco server para HTTPS (porta 443) neste arquivo.
